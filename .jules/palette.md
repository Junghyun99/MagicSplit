# Palette's Journal

- The site is a static dashboard generated by `docs/js/main.js` fetching `data/status.json`.
- Changes must be done in `docs/js/main.js` and `docs/css/style.css`.
- As the site uses `lang="ko"`, profit should be red and loss should be blue, according to KRX market conventions.
