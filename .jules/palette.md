The dashboard uses vanilla JavaScript to render `docs/data/status.json` dynamically into the HTML.
We should make sure generated artifacts like `docs/data/status.json` or `config.json` are not committed when creating UI improvements.
We should add ARIA labels for accessibility when representing positive/negative color-coded values.
