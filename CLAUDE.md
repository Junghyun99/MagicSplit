# MagicSplit - 분할매수/?�절 ?�동매매 �?

## ?�로?�트 개요
종목�?매수가 ?��?%???�라 분할 매수(물�?�? ?�는 매도(?�절)�??�동 ?�행?�는 Python ?�레?�딩 �?
차수(Level) ?�스?�으�?매수/매도�?계단?�으�?관리하�? ?�레?�링 ?�톱�??�적 ?�매??기�??�로 변?�성???�?�한??

## 기술 ?�택
- Python 3.10
- pandas, requests, python-dotenv, PyYAML
- yfinance, pyarrow (백테?�트 ?�세 캐시)
- pytest + pytest-cov (?�스??

## 주요 명령??
?�영???�업(�??�행/백테?�트/?�동매매/?�세 ?�운로드)?� GitHub Actions ?�크?�로?�로 ?�행?�다 (CI/CD ?�션 참조).
로컬?�서 ?�주 ?�는 명령??
- ?�존???�치: `pip install -r requirements.txt`
- ?�스?? `pytest` (Windows ?��??? `$env:PYTHONUTF8=1; pytest`)
- 커버리�? ?�함 ?�스?? `pytest --cov=src tests/` (80% ?�상 준???�수)
- �?1???�행: `python -m src.main` (`config_overseas.json_PATH`�?�?��/?�외 ?�택)
- ?��????�합: `python scripts/reconcile_positions.py`

## ?�로?�트 구조
```
src/
?��??� main.py              # MagicSplitBot 진입??(?�일 계좌, config_overseas.json_PATH�?�?��/?�외 ?�택)
?��??� config.py            # ?�커-거래??매핑, ?�프???�정, KIS ?�증
?��??� strategy_config.py   # config_*.json + presets.json 로더 -> StockRule 리스??
?��??� core/
??  ?��??� engine/          # base (MagicSplitEngine), registry
??  ?��??� logic/           # split_evaluator (분할 매수/매도, ?�레?�링 ?�톱, ?�적 ?�매??
??  ?��??� interfaces.py    # 추상 ?�터?�이???�의
??  ?��??� models.py        # ?�메??모델 (PositionLot, StockRule, SplitSignal ??
?��??� infra/
??  ?��??� broker/          # KIS domestic/overseas/mock 브로�?
??  ?��??� data.py          # YFinanceLoader (?�택??
??  ?��??� repo.py          # JsonRepository (positions.json, status.json, history.json)
??  ?��??� notifier.py      # SlackNotifier, TelegramNotifier
?��??� backtest/            # runner, cache (parquet), fetcher (yfinance), components
?��??� utils/               # TradeLogger
scripts/
?��??� manual_trade.py      # Actions/CLI?�서 ?�동 매수·매도 주문
?��??� reconcile_positions.py  # ?�계�??�고?� positions.json ?�합
tests/                   # ?�스??(80% 커버리�? ?�구)
docs/                    # ???�?�보??GitHub Pages) + config-editor + data ?�??
config_domestic.json     # �?�� 종목 매매 규칙
config_overseas.json     # ?�외 종목 매매 규칙
presets.json             # 차수�?배열 공유 ?�리??(?�택)
```

## ?�키?�처 규칙
- Clean Architecture ?�턴: core(?�메?? -> infra(?�프?? 방향?�로 ?�존
- core/interfaces.py???�의??추상 ?�터?�이?��? ?�해 ?�존??주입
- ?�진 ?��??�트�??�턴: `@register_engine` ?�코?�이?�로 ?�진 ?�록
- ?�일 계좌: .env??KIS ?�증?�로 �?��/?�외 브로�?각각 ?�성
- �?��/?�외 ?�립 ?�용: `config_overseas.json_PATH`�??�느 마켓???�릴지 ?�택, 별도 브로커·�??�소·?�진 ?�스?�스�??�전 분리
- 종목�??�차 ?�행: ?��? -> 주문 -> ?��???반영 -> ?�음 종목

## ?�심 ?�고리즘 (MagicSplit)
- 종목�?`config_domestic.json` / `config_overseas.json`???�의??매매 규칙(StockRule)???�라 ?�작
- 차수(Level) ?�스?? �?매수 �?PositionLot)??level(1~max_lots)??부?�하??추적
- **마�?�?차수�??��?**: 가???��? level??매수가 ?��??�재가 %�??�단
  - ?�승 M% ?�상 -> 마�?�?차수 매도 (차수 감소, ?? Lv3->Lv2가 마�?�?
  - ?�락 N% ?�하 -> ?�음 차수 매수 (차수 증�?, ?? Lv3->Lv4 추�?)
- **?�레?�링 ?�톱** (`trailing_drop_pct` ?�정 ??:
  - 매도 ?�계�?`sell_threshold_pct`) ?�달 ???�성??-> `trailing_highest_price` 추적 ?�작
  - ?�후 고점 갱신???�라가�? 고점 ?��?`trailing_drop_pct`% ?�락?�면 매도
  - 최소 ?�절??보장?�면??추�? ?�승분도 가?��???구조
- **?�적 ?�매??기�? (기�?가 ?�향)**:
  - 직전 매도가(last_sell_price)가 마�?�?차수 매수가보다 ?�으�?매도가�?매수 기�??�로 ?�용
  - ?�레?�링 ?�톱?�로 ?�소보다 ?�게 매도???? ?�래 그리?�까지 기다리�? ?�고 매도가 ?��??�락 ??즉시 ?�매??
- **?�진??가??* (`reentry_guard_pct`):
  - ?�량 �?�� ??직전 매도가 ?��?X% ?�락?�야 1차수 ?�진???�용 (?�수, ?? -0.1)
  - None?�면 가??비활??(?�음 ?�이??즉시 ?�진??가??
- **차수�?배열 + presets.json**:
  - `buy_threshold_pcts` / `sell_threshold_pcts` / `buy_amounts` / `trailing_drop_pcts` 배열�?차수마다 ?�른 ?�계�?금액 지??가??
  - 배열 길이가 차수보다 짧으�?마�?�?값으�?clamp
  - `presets.json`??공통 ?�정???�의?�고 종목?�서 `"preset": "?�름"`?�로 참조 (종목 ?�드가 preset??override)
- **?�입 비율 ?�한** (`max_exposure_pct`):
  - 계좌 �??�산 ?��?종목�?최�? ?�입 비중???�한 (?? 20.0 = 20%까�?�??�입)
  - 매수 ??`(?�재 보유 ?��???+ 매수 ?�정 금액) / �??�산`???�한???�으�?매수 차단
  - **글로벌/개별 계층**: `global.max_exposure_pct`�?기본값으�? 종목�??�정???�으�??�버?�이??
  - None?�면 비중 ?�한 ?�음 (?�위 ?�환)
- **???�이?�에 ??종목??매도 OR 매수 �??�나�?* ?�행 (매도 ?�선)
- 종목�??�차 ?�행: ??종목 ?��?->주문->반영 ???�음 종목
- ?�러 종목 �?매도 ?�호�?먼�? ?�행????매수 진행 (?�금 부�?방�?)
- 차수 ?�름 ?�시: 1,2,3,4,3,4,5,6,5,4,3,2,3,4,5 (?�르?�내리락)

## ?�경변??(.env)
- `KIS_APP_KEY` - KIS API ????
- `KIS_APP_SECRET` - KIS API ???�크�?
- `KIS_ACC_NO` - KIS 계좌번호
- `IS_LIVE` - ?�거???��? ("true" / "false", 기본�? "false")
- `SLACK_WEBHOOK_URL` - Slack ?�림
- `config_overseas.json_PATH` - 매매 규칙 ?�일 경로 (기본�? `config_overseas.json`. �?��??`config_domestic.json` 지??
- `PRESETS_JSON_PATH` - ?�리???�일 경로 (?�택, 기본?� config ?�일�?같�? ?�렉?�리??`presets.json`)
- `PYTHONUTF8` - Windows ?�경?�서 ?��? 로그 ?�코???�류 방�???(?�수: `1` ?�정)

## 로컬 ?�경 ?�정
1. `.env.example` ?�일??`.env`�?복사
2. `.env`???�제 KIS API ??�?`PYTHONUTF8=1` ?�정 ?�인
3. VS Code ?�용?? `.vscode/settings.json`??`terminal.integrated.env.windows` ?�정 권장 (?�동 ?�용??
4. ?��??�별 UTF-8 모드 ?�동 ?�성??(명령???�행 ??:
   - **PowerShell**: `$env:PYTHONUTF8=1; python -m pytest`
   - **CMD**: `set PYTHONUTF8=1 && python -m pytest`


## CI/CD
GitHub Actions ?�크?�로??
- `python-test.yml` - main 브랜�?Push/PR ???�위 ?�스??(80% 커버리�? ?�수, ?�패 ??Slack ?�림)
- `trading-bot-overseas.yml` - ?�외 매매 �?(?�재 ?��?�?비활?? manual dispatch). `config_overseas.json_PATH=config_overseas.json`
- `trading-bot-domestic.yml` - �?�� 매매 �?(?�재 ?��?�?비활?? manual dispatch). `config_overseas.json_PATH=config_domestic.json`
- `manual-trade.yml` - Actions UI?�서 ?�동 매수/매도 주문 (market_type, ticker, action, quantity ?�력)
- `run-backtest.yml` - 백테?�트 ?�행 (?�작/종료?? 초기 ?�본, 마켓 ?�???�택). 결과�?`docs/data/backtest/`??커밋
- `download-market-data.yml` - yfinance�?종목 ?�세�?받아 `src/backtest/cache/`??parquet 캐시�?커밋

## ?�크?�로??규칙
- 코드 ?�정 ?�청 ???�업 브랜치�? ?�성?�여 커밋?�고 PR ?�성까�? ?�료?�다
- PR ?�성 ??로컬 ?�스??pytest)�??�행?�여 ?�과 �?커버리�?(80% ?�상)�??�인?�다
- CI/빌드 ?�러 발생 ??로그�?분석?�여 ?�인???�악?�고 ?�정?�다
- PR 리뷰가 ?�으�??�용??검?�하??반영?�고, 미반???�에??�??�유�?명확???�명?�다

## 주의?�항
- .env ?�일?� ?��? 커밋?��? ?�을 �?
- ?�거???��???.env??`IS_LIVE` ?�드�??�정
- 매도 주문??먼�? ?�행????매수 진행 (?�금 부�?방�?)
- **?�코???�환??*: 로그, 주석, 문자?�에 ?�수 ?�니코드 문자(?? `??)�??�용?��? 말고 ?��? ASCII 기호(?? `->`)�??�용??�?
- 코드 ?�집?�때??반드???�일??먼�? ?�어??
- `docs/js/*.js` ?�는 `docs/css/*.css` �??�정???�는 반드??`docs/index.html`???�당 ?�일 `?v=` ?�라미터???�께 ?�릴 �?(브라?��? 캐시 무효??
  - ?�식: `?�짜-?�번` (?? `20260425-1`, 같�? ???�수????`20260425-2`�?증�?)
